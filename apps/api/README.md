# ModelForge API

Python 3.13 FastAPI service. Source: [`src/`](src/).

From the **repository root**:

```bash
make install-dev
make api
```

Docker build context is the repo root — see [`docker-compose.yml`](../../docker-compose.yml) and [`Dockerfile`](Dockerfile).

Full documentation: [../../README.md](../../README.md).
