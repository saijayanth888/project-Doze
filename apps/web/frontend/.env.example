# Empty = same-origin /api (Docker nginx or Vite proxy). Set full URL only if API is elsewhere.
VITE_API_BASE_URL=
VITE_WS_URL=ws://localhost:8000
# Build-time API key. Leave empty to require runtime override via
# localStorage["modelforge_api_key"].
VITE_MODELFORGE_API_KEY=
